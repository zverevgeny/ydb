[x] 1. Feature flag in KQP config — added `EnableHttpEgress = 312 [default = false]` to `ydb/core/protos/feature_flags.proto`.
[x] 2. §7 decision record — documented in `ydb/library/yql/dq/actors/http/DESIGN_DECISIONS.md` (ADR covering all 6 open questions with decisions, rationale, and code references).
[x] 3. Caller-death cancellation (PassAway/poison) — implemented `PassAway()` override that calls `CancelAllPendingRequests()` to clean up all pending requests, counters, and per-host tracking before actor shutdown. Handled via `cFunc(NActors::TEvents::TEvPoison::EventType, PassAway)` in StateFunc.
[x] 4. DNS-rebinding resolve-and-pin — implemented `ResolveAndPinHost()` in `http_egress_security.cpp` using `getaddrinfo()` to resolve hostname before request and validate the resolved IP against SSRF blocks. Added `PinnedIP` field to `TPendingRequest` for tracking.
[x] 5. Authorization/secret redaction — implemented `IsSensitiveHeader()` and `RedactSensitiveHeaders()` in `http_egress_security.cpp`. Error messages in `ValidateRequest()` now redact sensitive header names using `IsSensitiveHeader()` → `"[REDACTED_NAME]"`. Added 5 unit tests: `TestIsSensitiveHeader`, `TestRedactSensitiveHeaders`, `TestRedactSensitiveHeadersEmpty`, `TestRedactSensitiveHeadersNone`. Total: 39 tests pass.
[x] 6. TLS verification default — documented in DESIGN_DECISIONS.md §7.7: TLS verification always enabled by default (system CA bundle). No VerifyTLS flag exposed in v1. Custom CA support deferred to later phase.
[x] 7. TEST_PLAN.md — already correctly marks RES-007 as `false (not reserved in v1)`, SSRF-ATK-001/003-005/007-008 as `⏳ Planned`, RESP-001..004 as `⏳ Planned`, TO-001..003 as `⏳ Planned`.
[x] 8. COVERAGE.md — bumped to 35 tests, added regression tests (IPv6 with allowlist, IPv4-mapped IPv6, case-insensitive host policy).